import React from "react";
import {FilterModal} from "../components/FilterModal";

const Home = props => {
    return (
        <div>
            <FilterModal />
        </div>
    );
};

export default Home;